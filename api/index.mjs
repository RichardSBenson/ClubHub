/**
 * Vercel entry point. One function, all routes.
 *
 * Deliberately thin: everything real is in packages/, so this file never needs
 * editing from a phone.
 */
export { default } from '../packages/api/server.mjs';
